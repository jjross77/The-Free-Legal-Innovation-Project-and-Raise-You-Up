# -*- coding: utf-8 -*-
"""
Created on Tue Jan 30 16:32:11 2024

@author: yyyyyyyyyyyyyyyyyyyy
"""

#print()
#self.listbox4.insert(tk.END," dog\n dog\n /n dog\n/n ")
#self.listbox1.delete(1, tk.END)
#self.listbox1.delete(0, tk.END)
#content=self.text_box1.get('1.0', 'end') # textbox1 is the method window
   # objectives window is loading but the other one is not
   # need to figure out what is different to diagnsois problem
   # info for this prolbem at 544:::
   # why is the widget in frame 3 not reacting fraem not reacting 
   # objective is to get the list to shwo up in the lsitbox or to get
   # to get anything to show up on lsitbox
   # need to see if this is what a debugger does and start using it
   #if i==1 and j==0:# existing galaxies to draw on  # on left side all the way down
   #    quad.frame3 = tk.Frame(master=window, relief=tk.RAISED, borderwidth=1)
   #    existing_objects_in_database_list=quad.import_tools_problems_or_existing_ideas(7)
   #   quad.side_lists("Possible Objects",existing_objects_in_database_list,quad.frame3)
   #label_list=self.import_tools_problems_or_existing_ideas(category)
   # need to fix category names
   

   #for  frame, category in frames.items():
   #    for widget in frame.winfo_children():
   #        widget.destroy()  
   #    if category ==15:
   #        print('15')
   #label = tk.Label(master=frame_name,text=title_of_text, fg="black", bg=self.background_label_color)
   #label.grid(row=0, columnspan=3)
   #label.config(font=('Times New Roman',8),bg='light yellow')
   #if title_of_text == "Current Method Steps":
   #    self.listbox4 = tk.Listbox(frame_name,width=60)
   #    self.listbox4.grid(row=1,column=0, columnspan=3)
   #    self.listbox4.bind('3',self.transport_to_method_window)
   #    self.listbox4.bind("4",self.switch_to_previous_galaxy)
   #    for i in range(10):
   #        dog='woo'
   #        self.listbox4.insert(tk.END,f'{dog}')
   # listbox4 intitally going to try a different name
   # need to change the frame name and see the whole
   # pipeline to diagnsosis the prolbem
   # frame 3 intitally created then delete widget create listbox4
   # then try to fill listbox other listboxes are working this one is not
   # trying to figure out why this one is not uploading the list
   # try changing names of variables
   # try to see differences between this is list boxes for objectives
   #
   